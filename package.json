{
  "name": "atithi-platform",
  "version": "0.1.0",
  "private": true,
  "description": "One-stop Indian hospitality platform: verified direct hotel booking + AI receptionist for missed calls",
  "workspaces": [
    "packages/*",
    "backend",
    "frontend",
    "infrastructure"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "dev:web": "npm run dev -w frontend",
    "dev:api": "npm run dev -w backend",
    "deploy": "npm run build -w packages/shared && npm run build -w backend && npm run deploy -w infrastructure",
    "deploy:web": "npm run build -w frontend && npm run deploy:web -w infrastructure"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
